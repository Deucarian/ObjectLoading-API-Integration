# API Helper Downloader Sample

`ApiHelperBridgeDownloaderSample.cs` shows the composition point for replacing Object Loading's default UnityWebRequest downloader with `ApiHelperObjectDownloader`.

The sample still expects a resolved AssetBundle URL. It does not resolve backend URLs or use Session Helper.
